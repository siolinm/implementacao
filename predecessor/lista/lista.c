#include "lista.h"

// Objeto * lista;

// Objeto * criaLista(int n){
//     lista = (Objeto *)malloc(n*(sizeof(Objeto)));

//     return lista;
// }

// Bool compare(const void * a, const void * b){
//     Objeto * ia, *ib;
//     ia = (Objeto *)a;
//     ib = (Objeto *)b;
//     return ia->valor > ib->valor;
// }

// void predecessor(int i){
//     if(i > 0){
//         printf("O predecessor do ponto %d é o ponto %d\n", i, i-1);
//         printf("Trajetória de %d: %g*t + %g\n", i, lista[i].traj.a, lista[i].traj.b);
//         printf("Valor de %d: %g\n", i, lista[i].traj.a*getTime() + lista[i].traj.b);
//         printf("Trajetória de %d: %g*t + %g\n", i-1, lista[i-1].traj.a, lista[i-1].traj.b);
//         printf("Valor de %d: %g\n", i-1, lista[i-1].traj.a*getTime() + lista[i-1].traj.b);
//     }
//     else    
//         printf("Esse ponto não possui predecessor\n");
        
// }
