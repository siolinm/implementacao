#include<stdio.h>
#include"heap.h"
#include"certificados.h"
#include"time.h"
#include"menu.h"

int main(){
    Objeto * lista = NULL, * heapCert = NULL;
    int n = 0;
    
    return menu(lista, heapCert, n);
}